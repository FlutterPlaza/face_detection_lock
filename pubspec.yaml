name: face_detection_lock
description: This widget will unlock screen when a face is detected looking at app.
version: 0.0.1+1
repository: https://github.com/FlutterPlaza/face_detection_lock/tree/v0.0.1+1
homepage: https://flutterplaza.com

environment:
  sdk: '>=2.18.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  camera: ^0.10.0+4
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.1
  freezed_annotation: ^2.2.0
  google_ml_vision: ^0.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  freezed: ^2.2.1
  build_runner: 

flutter: